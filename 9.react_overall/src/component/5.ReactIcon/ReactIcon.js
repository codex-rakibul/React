import React from 'react';
import { FaReact } from "react-icons/fa";

const ReactIcon = () => {
    return (
        <div>
            <h1 className='' style={{padding:'5px'}}>Welcome to React {<FaReact/>}</h1>
           
        </div>
    );
};

export default ReactIcon;